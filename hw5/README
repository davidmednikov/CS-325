This program is written in Python, so the only things that should be required are:
1) Python must be installed on the machine where the program is run.
2) The input file provided in the runtime argument must exist in the same directory as the program.
