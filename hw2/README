This program is written in Python, so the only things that should be required are:
1) Python must be installed on the machine where the programs are run.
2) There must be a file called data.txt containing rows of integers for the programs to read and sort.
